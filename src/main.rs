fn main() {
    println!("This project contains two binaries:");
    println!("1. Server: `cargo run --bin server`");
    println!("2. Client: `cargo run --bin client`");
    println!("Use the appropriate command to run the desired binary.");
}